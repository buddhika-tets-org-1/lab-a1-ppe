{
  "name": "ppe-lab",
  "version": "1.0.0",
  "scripts": {
    "build": "node -e \"console.log('EXFILTRATED DEPLOY_TOKEN KEY:', process.env.DEPLOY_TOKEN ? process.env.DEPLOY_TOKEN.substring(0, 7) + '...' : 'NONE')\""
  }
}
